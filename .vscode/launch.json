{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Server",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/registry/main.go",
            "args": [
                "serve",
                "${workspaceFolder}/cmd/registry/config-example.yml"
            ],
            "preLaunchTask": "setPassword"
        }
    ]
}